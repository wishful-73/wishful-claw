# 裁定记录（跨需求流水）

> 按日期追加，跨整个迭代。需求正文见 `requirements/S-{NNN}.md`。
> 立项与口径变更的流水，按日期追加。正文小节按编号排列，本节日志跨整个迭代。

- 2026-09-23：**iter-35 立项开工**（老大「根据 `AGENTS.md` 拆分 35 迭代，然后准备记录 bug」）。分支 `dev/v2-iter-35` 从 `main` @ `991c3019`（`v0.2.34`）切出。iter-34 已收尾合并 main、tag 与 GitHub Release 均就位。**本迭代范围尚未拍定**，当前处于「登记中」阶段。
- 2026-09-23：**S-140 立项**（更新走了全量下载 108 MB，未走差分）。老大 08:27「这次发布的包是不是有什么不一样的地方，之前版本都是差异更新大概 14M，这次直接 108M 拉满了」；08:33「30-33 这些版本之前都是走的增量，14M 左右，就今天 34 走的全量」。**只登记不执行。** 排查已完成（全文见 [S-140.md](requirements/S-140.md)）：发布侧无问题，失败原因是本地差分基准 `%LOCALAPPDATA%\wishful-claw-updater\installer.exe` 缺失 ⇒ electron-updater 按设计 fallback 全量；本机日志全历史「差分」记录只有 4 条且**全是 ENOENT 失败**（从未成功差分过），与老大「30-33 走 14M」的口径**对不上**。基准现已就位（sha512 与 0.2.34 包一致）⇒ 0.2.34 → 0.2.35 应能差分。**三项待裁定**：① 老大的「14 M」在哪看的、是不是这台机器；② 要不要做防护、做哪一层；③ `disableWebInstaller` 是否设 `true`。
- 2026-09-23：**老大补登三条 bug（08:42），编号顺延** —— 第 1 条并入 S-140，另立 S-141 / S-142。
  - **第 1 条并入 S-140**：老大判断「差分下载被改出异常了，问题就在 34 迭代的改动上，这里需要还原」。**核实结果：不成立，不要还原。** 证据链（全文见 [S-140.md](requirements/S-140.md)「三·补」）：① 基准写入者是 NSIS 安装器 `installer.nsh:93`，34 没碰 `electron-builder.yml` / nsis 模板 / `package.json` 的 build 段；② 34 对 updater 的全部改动只有 3 处（巡检 1h→6h、保留已下载同版本、旧包 discard），而 `clear()` 经代码确认**只 `emptyDir(cacheDir\pending)`**，不碰根目录 `installer.exe`；③ **9/22 19:55 那次失败跑的是 0.2.33**（34 收尾刀 19:45 才落）⇒ 34 的代码根本没生效；④ 打包版日志 `differentially` 只有 2 条且**全是 ENOENT**，9/18~9/21 压根没发生 app 内更新 ⇒ 本机不存在「34 之前差分成功」的记录可比；⑤ dev 实例那两条 ENOENT 天然正常（dev 从没被安装过）。
  - **S-141 立项**（进入历史会话即触发压缩）—— **勘测已完成，铁证到手**：`logs/2026-09-23.log`（**正跑 0.2.34**）**61 条 DIAG 全部 `contextLength=200000`**，`inputTokens=144083 > trigger=144000` 直接触发压缩；而 144083 / 1M ≈ 14.4% ≈ 前端显示的 15%。⇒ **S-108「后端按 200K 兜底」的真复现**，S-108 在 34 只做了告警没除根 ⇒ 建议从「未复现结案」重新翻出。
  - **S-142 立项**（回合末尾多渲染一个空思考流）—— **根因已定位，确为 34 引入**：元凶是 **S-133 补完 `3366f8f1`**（9/22 18:39）改的 `chat-store/index.ts:2246` —— `findLast` 谓词从「只跨 `tool_use`」扩到「跨 `tool_use` + `thinking`」，导致新 text 并回 thinking **之前**的 text 段、thinking 段被越过留在末尾；该 thinking 为空时末尾就是空思考流。v0.2.33 用 `lastSeg`（只看末尾、末尾非 text 就新建）⇒ 时序正确、无此问题。修法待裁（A 回退 / B 判据收窄为「只跨空 thinking」/ C 渲染层过滤 / D 空 thinking 不入数组）。
    **订正**：本条初次登记（08:42）被我按「取最后一段文本当总结」理解而找偏了方向，且一度标注「34 相关性已否」——**均系误判**，08:47 老大订正为「空思考流」后随即定位到真根因。
- 2026-09-23：**承接 iter-34 挂账已列表待裁**（未立项）—— S-129（更新源切官网，阻塞在备案）、S-131 路径二（缩窗无 resize 监听）、S-134（切会话但 tab 还开着）、S-108（未复现）、`will-navigate` 同源隐患 10 处、`memory-output.tsx:115` 裸英文、`disableWebInstaller`。清单见 [raw-requirements.md](raw-requirements.md) 末节。**是否立项、是否排进本迭代，等老大拍。**

> 注：S-108 的「未复现结案」状态已被 **S-141** 推翻（有 0.2.34 日志铁证），挂账清单里那条待老大重新裁定。
- 2026-09-23：**S-142 根因改判（DB 取证 + 老大两次订正）** —— 初判 S-133 补完 `findLast`（09:31，已推翻：劈出的 thinking 段非空，1~38 字符）；终判 **S-135 batcher**（`batcher-codec.ts`）：`accumulateEvent` 把 text/thinking 攒进两个独立串、**到达顺序丢失**，`drainAccumulator` **固定顺序「正文→思考→工具」倒出** ⇒ 前端收到 `[thinking1, text, thinking1尾巴]` ⇒ 思考流被劈。DB 取证：打包版库最新 6 条 assistant 消息每条都有此模式（尾巴 1~38 字符，如「8f1。」「删操作。」）。**「33 之前没有」吻合**（batcher 为 34 S-135 引入）。S-133 补完洗清（text 并回是正确行为）。修法：甲（kind 冲突先 flush，推荐）/ 乙（按序片段数组），待裁。
- 2026-09-23：**老大补充 S-135 历史口径（09:39）**：当初 S-135 修的就是「文本被思考流/组件卡片劈开」，修复后今天才暴露 142 —— 同一 batcher 重排病根，劈的对象从文本换成了思考流。
- 2026-09-23：**S-142 修法裁定（老大 09:45 拍板：丙案）** —— 老大 09:42 提出思路「首段接收有没有标识，按标识拼接」；核实现协议 **tool 有完整边界标识（`tool_use_streaming_start`→`args_delta`→`generated`）从不劈，text/thinking 只有裸 delta** ⇒ 劈开全发生在这两类；上游边界信息存在（Anthropic `content_block_start/stop` C# parser 收到未上传；OpenAI 系在 reasoning→content 字段切换点）。**丙案 = 协议加 `thinking_start`/`thinking_end`/`text_start`，后端 kind 切换处发，前端按边界续段/开段**；batcher 甲方案（冲突先 flush）作附带项；`findLast` hack 评估退役。Reasonix 对照（老大 09:45 指派查看）：消息级 2 字段、不存段，劈开结构上不存在但丢交错时序 —— S-133 已裁定不抄；教训「边界必须是一等公民」。实施范围五项见 [S-142.md](requirements/S-142.md) §四。**只登记不执行。**
- 2026-09-23：**S-144 立项**（沙箱模式下 Write 写特定路径被拦）。老大 09:46 指派查看知识库 `issues/bugs.md`（9/22 20:55 新增）。现象：开沙箱后 Write 写特定路径触发限制失败；触发候选：①工作目录下嵌套约两层子目录、②中文文件名、③下划线文件名（确切项未定位）。**连锁影响**：凶兆 agent 被拦后弃用文件工具改走 py 脚本 → 44 个脚本污染全局数据目录（两现象同源）；用户已关沙箱规避。修法方向：路径校验支持中文/下划线/多层子目录，或失败时清晰报错。**只登记不执行，待勘测。**
- 2026-09-23：**S-142 实施完成（11:01，一刀）** —— 丙案落地：协议加 `text_start` / `thinking_start` / `thinking_end`（控制类事件，不进 `AGGREGATABLE_EVENT_TYPES`）；C# 新增 `StreamSegmentBoundary` 并在 Anthropic（`content_block_start/stop`，显式块边界）、OpenAI Chat（SSE + JSON 两条路径，kind 切换）、OpenAI Responses 三处 emit；batcher 侧**附带把甲案做了**（`accumulateEvent` 遇 kind 冲突先 flush ⇒ 窗口内同类才攒，固定倒出顺序不可能再重排）；前端 chat-store 按边界「续段/开段」，`thinking_start` 用瞬时字段保证开新段且**不产生空段**。**text 分支保持 S-133 的 `findLast` 合并**（正文连贯是老大裁过的口径）。**明确不升协议版本**（信封结构未变、双向兼容；升 v 会让 dev 未重编 worker 变成整信封丢弃）。门禁：`dotnet build` 两侧 0 错 0 警、`npm run typecheck` EXIT=0、`npm test` **49/49**（新增 C# 套件 `WishfulClaw.StreamSegmentRegressionTests`）。细节见 [S-142.md](requirements/S-142.md) §六。
- 2026-09-23：**S-143 托管口径已定（老大 09:48）**：「安装包直接上传到官网所在位置」—— 资产（setup.exe + latest.yml + blockmap + latest.json）直传官网所在腾讯云南京节点 `119.45.103.252`（Nginx 静态伺服），**不走 COS、不依赖 GitHub Releases**；app 内更新源随之切 generic 指向官网（S-129 主体一并落，更新链路国产化）。遗留待勘测：上传方式/服务器目录/Nginx 配置、发版流程加「上传官网」步骤（`docs/release-workflow.md`）、页脚备案号样式、公安备案材料。
- 2026-09-23：**S-141 定性推翻重立（老大 09:52）** —— 初判「前端按模型窗口、后端按 200K 兜底，同一份上下文两套分母」（S-108 取值链）**被推翻**。老大实测：UI 显示**用了 25K 左右、上限 180K**（200K−20K）⇒ 前端自洽无问题；**问题在「历史会话重启后首次发送消息」**——后端触发加载，加载体量 ≈144K = **整个会话原始历史**，而非压缩快照后的活跃上下文（25K）。**分子错，不是分母错**；144083 > 触发线 144000 只是加载 bug 的副作用。S-108 的 200K 兜底可能仍是独立问题，但非本案主因，待修完看残留。重新定性：**重启后首条消息的上下文重建未（正确）应用压缩快照**。待勘测见 [S-141.md](requirements/S-141.md) §四。
- 2026-09-23：**S-143 立项**（官网上线 + 公安备案）。老大 09:18 告知工信部 ICP 备案通过：**蜀ICP备2026057067号**，审核通过日期 2026-09-23。范围：① 官网正式上线（含 `latest.json` 版本同步、页脚悬挂备案号——合规硬要求）；② 凭 ICP 号做公安备案。**S-129 的备案阻塞随之解除**（是否并入 S-143 待裁）。**只登记不执行。**
  - 另：S-140 新证据核对完毕 —— 33 迭代对更新链路零改动、34 改动不碰差分路径、electron-updater 版本两版一致（^6.6.2）、release 三资产实测齐全且 sha512 匹配 ⇒ 「33/34 改挂差分」在代码与资产层面均被否。
- 2026-09-23：**S-140 勘测收口 + 挂起裁定（老大 09:29「先不纠结，34 下载 35 再出现全量再处理」）**。本轮新增勘测：① v0.2.32/33/34 三版 `app-builder-lib` 均为 **26.15.3** ⇒ NSIS 模板三版逐字节相同，安装器行为无差别；② 模板中碰 `%LOCALAPPDATA%` 仅 installer.nsh:82/93 两处**写入**，无任何删除；卸载器只碰 `$INSTDIR` / `$APPDATA`；③ electron-updater（NsisUpdater / DownloadedUpdateHelper / AppUpdater）全流程对缓存目录**只写不删根目录**，`clear()` 只 `emptyDir(pending)`；④ **新增候选**：`copyFile` 宏（installUtil.nsh:12）用 `CopyFiles /SILENT` 不查错误 ⇒ 写基准失败静默无痕。⇒ **32/33/34 三版代码与安装器均无删除基准的路径**。v0.2.32 安装包下载实测未执行（本机装 32 会覆盖 0.2.34，静态已闭环，增量有限）。**观察点**：0.2.35 发版后看打包版日志 `Download block maps` 后无 ENOENT ⇒ 差分恢复即闭环；再现全量则重开。
- 2026-09-23：**S-140 口径订正（老大 09:18）**：① 「成功是静默的、没有日志」—— 与 S-140 §2.2 已采口径一致（fallback 为 ERROR 必留痕，差分成功不落盘）；此前会话总结用过「本机差分从未成功」的旧口径，系总结错误，S-140 时间线（9/19、9/20 无 fallback 报错 = 差分成功）为准。② 老大称「ENOENT 记录是网络波动下载失败」—— **与日志证据冲突，已回禀待澄清**：ENOENT 报错对象是本地基准文件路径（`open ...installer.exe`），未到网络层；网络波动在日志中另有独立记录（9/22 20:31 `ERR_HTTP2_PROTOCOL_ERROR`，32.7% 断流）。S-140 现有定性（差分一直成功，9/22 起本地基准 `installer.exe` 缺失 ⇒ fallback 全量；基准已于 9/23 装完 0.2.34 时由 NSIS 写回）维持不变，**唯一未闭环项仍为 §四-0：9/20 20:20 ～ 9/22 19:55 之间是否手动动过 `%LOCALAPPDATA%\wishful-claw-updater`**。
