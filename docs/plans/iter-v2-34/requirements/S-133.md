# S-133 · 流式正文被思考块 / 工具组件从中间打断

- 2026-09-22：**老大提出**（原话：「前端渲染的时候，有时候输出的文本会被思考 或者 组件的渲染打断，也就是一段正常的文本渲染 中间突然插了一个组件的渲染。」）。
- **类型**：UI 渲染 / 流式时序；**间歇性**（「有时候」），非必现。

**现象（老大 2026-09-22 二次澄清，用他的原话）**：「文本输出到一大半，说准备调用工具，然后工具两个字还没输出出来，工具组件已经显示了。……是先有正文，然后调用工具的情况有问题，正常调用工具，后输出文本的是没有问题的。」即 —— **先出正文、再调工具**时，**同一段连贯正文被工具卡从中间截成两截**。

**方向性（老大实测，本需求的关键判据）**：

- ✅ **先调工具、后出文本 ⇒ 正常**：store 里 segments 长成 `[tool, text]`，渲染「卡 + 文本」，符合预期。
- ❌ **先出文本、再调工具 ⇒ 出问题**：store 里 segments 长成 `[text1_1, tool, text1_2]` —— `text1_1` / `text1_2` 是**同一段话**被工具调用劈成的两截。

> 这两种情形**互为镜像**，正是下面 `_flushStreamDeltas` 那个「追加还是新建」判据的两种输出 —— 所以「有时候」不是随机的，**取决于工具调用插在正文的哪个位置**。

**根因（第二版，修正前次误判）** —— ⚠️ 前次我把根因判给渲染层的 `splitProcessAndFinal`，**判错了**。老大 2026-09-22 的纠正点中要害：**内容形如 `[text1, tool]` 时就不该被劈开** —— 即**内容层**（store 组装 segment）本身就不该产出被劈开的数据，渲染层只是**放大**了它。

**真凶：`stores/chat-store/index.ts:2237-2249`（`_flushStreamDeltas` 的 text 分支）**

```ts
// Append to last text segment of current iteration, or create new
const lastSeg = msg.segments[msg.segments.length - 1]

if (lastSeg && lastSeg.type === 'text' && lastSeg.iteration === msg.currentIteration) {
  lastSeg.text = (lastSeg.text ?? '') + delta.text                                       // 追加
} else {
  msg.segments.push({ type: 'text', iteration: msg.currentIteration, text: delta.text })  // 新建
}
```

**判据太严 —— 只认「最后一个 segment 是 text」**。而工具卡会 `msg.segments.push({ type: 'tool_use', ... })` 插到**末尾** ⇒ 之后到达的 text delta 只能走 `else` **新建 segment**。

**流式时间线（同一次 LLM 响应 = 同一个 `iteration`）**：

| 步 | 事件 | `msg.segments` | 老大师到 |
|:--:|---|---|---|
| 1 | `text_delta` × N | `[text1_1]` | 正文在输出 |
| 2 | `tool_call_start` | `[text1_1, **tool_use**]` | 工具卡出现 |
| 3 | `text_delta`（续） | `[text1_1, tool_use, **text1_2**]` | **正文被劈开** ❌ |

**⇒ 与老大给的两种情形精确吻合（这才是真正的「镜像」）**：

- **先出文本、再调工具** ⇒ 步 3 走 `else` 新建 ⇒ `[text1_1, tool, text1_2]` ⇒ **劈开** ❌
- **先调工具、后出文本** ⇒ `[tool_use]` 收到 text delta ⇒ `lastSeg` 是 tool_use ⇒ 新建 ⇒ `[tool, text]` ⇒ **正常** ✅

**渲染层如何放大它**：`chat-message-blocks.ts:28-32` 把 `msg.segments` **原序**转成 blocks，再由 `splitProcessAndFinal`（`process-summary.ts:152-167`）**从尾部倒扫**切一刀 ⇒ `processItems = [text1_1, 卡]`、`finalItems = [text1_2]` ⇒ `text1_1` 留在卡上方、`text1_2` 掉到卡下方，**劈开被视觉放大**。⇒ **渲染层不用动**（对正确的 `[text1, tool]`，split 产出的「`text1` + 卡」本来就对）。

**已排除的两条**：

- **工具卡折叠吞文本**：`renderItems`（`index.tsx:370-393`）用 `i = run.endBlockIndex` 跳整段 run，但 `execution-outline.ts:394` 的 `endBlockIndex` 只更新到最后一个 tool_use，且 `:327-332` 遇 text / thinking 就 `closePendingRun()` 断开 run ⇒ run 区间内不含文本。**逻辑正确。**
- **正文打字机滞后**：`useStreamingRenderPool`（`hooks/use-typewriter.ts:68`）只被 `ThinkingBlock` 用；正文走 `StreamingMarkdownContent`（`markdown-renderer.tsx:455`）。**两者不是同一机制。**

**修法（待裁定）**：

- **A（推荐，治本）**：text delta 追加时**向前找**当前 `iteration` 内最近的 text segment；若它与末尾之间**只夹着 `tool_use`**（无 text / thinking 隔断），就**合并到它** ⇒ segments 保持 `[text1, tool]`，渲染为「正文 + 卡」。
- **B（治标）**：store 不动，改在 `splitProcessAndFinal` 里把同 `iteration` 的 text 合并渲染。⇒ 落库 / 复制 / 其它消费方**仍看到劈开的数据**。

我倾向 **A**：在**数据源头**消除劈开，且 `iteration` 判据能区分「同一响应内的碎片」与「跨轮次的独立文本」。

**⚠️ 一处需老大确认（决定 A 会不会误伤）**：老大认可的 **`[text1, tool, text2]`**，指的是哪一种？

- **(甲)** 第一轮说 `text1` + 调工具 ⇒ 工具跑完 ⇒ **第二轮**说 `text2` ⇒ 两者 `iteration` 不同 ⇒ **A 不会误伤** ✅
- **(乙)** **同一次响应**里模型主动分两段说（`text1 → 工具 → text2`）⇒ 同 `iteration` ⇒ A 会把它并成 `[text1, tool]`（**改变现状**）

若是 (乙)，则 A 需加附加判据（例如仅在「该工具仍 `running`、且 `text2` 紧跟 `text1` 到达」时才合并）—— 请老大定夺。

**独立性**：纯前端渲染层，**不依赖** S-107 / S-108 / S-132，可独立实施。

---

## 补完（2026-09-22）：正文被**思考块**从中间劈开

**触发**：老大 9-22 16:31 报「沙箱」两字被切开（「沙」在前、「箱」掉到下一行）；一张截图里一句话被切 **5 截**，每截都跟一条「🧠 已深度思考」绑在一起 —— 这是**第一版修复未覆盖的分支**。

**立项依据（老大原话）**：本文件第 3 行早写着「输出的文本会被**思考** 或者 **组件** 的渲染打断」—— **两个都提了**，第一版的根因分析只围绕 `tool_use`，thinking 这一半漏了。⇒ 老大 9-22 18:35 裁定：**算 S-133 补完，不另开编号**。

**DB 取证（坐实是数据层、不是渲染层）**：

- dev 库 `C:\Users\龚翼\.wishful-claw-dev\index.db`，目标消息 `wc-agent-1790065732576-7ecq04`（`meta` 长度 380092）。
- `msg.text` / `content`：**完整连贯** ——「往那个新会话发个简单可验证的任务——列目录并回执。」
- `meta.segments`：**已劈开**

```json
[ { "type": "thinking", "iteration": 1, "thinking": "…(大段推理)", "startedAt": 1790065734536, "completedAt": 1790065735405 },
  { "type": "text",     "iteration": 1, "text": "往那个新会话发个简单可验证的任务——列目录" },
  { "type": "thinking", "iteration": 1, "thinking": "Id.", "startedAt": 1790065735405, "completedAt": 1790065735456 },
  { "type": "text",     "iteration": 1, "text": "并回执。" },
  { "type": "tool_use", "iteration": 1, "toolCallId": "call_00_X54hwOu4GuRfjJLpXnou4375" } ]
```

⇒ **落库时就碎**，渲染层只是如实画出。`msg.text` 与 `msg.segments` 是两条独立累积路径（`chat-store/index.ts:2227` 的 `msg.text += delta.text` 不碰 segments）。

**根因（与第一版同源、同一行）**：`stores/chat-store/index.ts:2246`

```ts
// 改动前的判据（第一版）：
const targetTextSeg = msg.segments.findLast((seg) => seg.type !== 'tool_use')
```

判据只排除 `tool_use`，**思考段满足「不是 tool_use」⇒ 停在它上面** ⇒ `:2250` 的 `type === 'text'` 判否 ⇒ 走 `else` **新建** text 段。

**逐帧对得上**（起点 `segments = [thinking(A), text("…列目录")]`）：

| delta 到达 | 走的代码 | `segments` |
|:--:|---|---|
| `text`「往那个…列目录」 | `:2231` 末尾是 thinking ⇒ 标记其 completed；`:2246` 停在 thinking ⇒ 新建 | `[thinking(A), **text1**]` |
| `thinking`「Id.」 | `:2266` 末尾是 text ⇒ 新建 thinking 段 | `[thinking(A), text1, **thinking("Id.")**]` |
| `text`「并回执。」 | `:2246` **停在 thinking** ⇒ 判否 ⇒ 新建 | `[thinking(A), text1, thinking("Id."), **text2**]` ❌ |

**修法（`:2246-2248`，一行判据 + 注释）**：

```ts
const targetTextSeg = msg.segments.findLast(
  (seg) => seg.type !== 'tool_use' && seg.type !== 'thinking'
)
```

`ContentSegment.type` 只有 `'thinking' | 'text' | 'tool_use'` 三种（`stores/chat-store/types.ts:25`）⇒ 该谓词**等价于「找本 iteration 最近的一个 text 段」**，即第一版「跨过 tool_use 往前找」同一口径的延伸。

**误合并风险由 `iteration` 判据兜住**（`:2250` 的 `targetTextSeg.iteration === msg.currentIteration`）：`currentIteration` 由 `iteration_start` 事件驱动（`:907`），跨 iteration 一定新建段 ⇒ 上一轮的正文不会被并进这一轮。

> 守卫的边界（审查记档）：`:2219` 的 `if (!msg.currentIteration) msg.currentIteration = 1` 只是兜底 —— 若某轮 `iteration_start` 事件丢失或触发时该 msg 尚未就绪，各轮 text 段的 `iteration` 会都停在 `1`，理论上存在跨轮合并。此依赖是**既有**的（第一版判据同样依赖它），**非本刀引入**。

**参考实现对照（老大要求先看 Reasonix / OpenCowork）**：

| 项目 | 做法 | 结论 |
|---|---|---|
| Reasonix（`D:\koda\DeepSeek-Reasonix-main-v2`） | `reasoning?: string` + `content: string` —— **思考与正文是两个消息级字段，不按「段」切**；渲染固定把 reasoning 摆在正文之上（`Message.tsx:693-710`，`reasoningComplete` 驱动自动折叠，`reasoningDisplay.ts` 只做流式尾部截断） | **根治**：不存在交错，也就劈不开。代价是**丢掉「思考-正文-思考」的交替时序** —— 它压根不显示交错 |
| OpenCowork（`D:\koda\OpenCowork`） | `main/cron/cron-agent-background.ts:1303-1312` 的 `appendText` —— **只看末尾**（`last?.type === 'text'` 才追加，否则 push 新块） | **同源病根**：我们第一版正是从这条继承来再改的；它自己遇到 `text→thinking→text` 一样会劈。**参考价值：否**。（`:1314-1323` 的 `appendThinking` 多一个 `!last.completedAt` 判据，我们缺 —— 见下「残留」） |

⇒ **不抄 OpenCowork**（同一个病）；**也不改成 Reasonix 那样彻底分离**（要放弃 segments 时序模型与工具卡定位，改动面远超一个 bug 修复）。

**渲染层不改（订正我前次的判断）**：`process-summary.ts:152-167` 的 `splitProcessAndFinal` 从尾部倒扫、撞 thinking 就停 —— 但它切的是 **render item 边界**，**不会从字符中间劈**；本案的劈开完全由数据层产生。另 `think-parser.ts:54-82` 的 `normalizeStructuredBlocks` 本来就会合并**相邻**同类块，数据层不再劈开之后自然生效（中间夹着 thinking 才合不上，这正是劈开被视觉放大的原因）。

**历史数据不回填**：已落库的 segments 保持原样 ⇒ 老会话的历史消息仍会显示劈开，**新消息不再劈**。需要回填另议（要写迁移脚本，超出本刀）。

**残留（不修，前端不加 hack）**：

1. 同批 DB 证据里 `thinking("Id.")` 只有 **3 字符**、寿命 `startedAt→completedAt` **51ms**，异常短。上游为什么吐这种碎片未查 —— 若它本不该存在，本 bug 的形态会更简单，但**两条独立**，不在本刀。
2. `:2268` 的 thinking 追加判据缺 `!lastSeg.completedAt`（对比 OpenCowork `appendThinking:1318`）：一个已被标记 completed 的思考段若又收到 thinking delta，会被**复活续写**（`completedAt` 却留着）。既有行为，本刀不碰。
3. `:2266` 的 thinking 分支**同样是「只认末尾」**（`lastSeg.type === 'thinking'` 才追加，否则新建）：同一 iteration 内 `thinking → text → thinking` 会产生多个思考段。**性质与 text 不同** —— 用户看到的只是多个「已深度思考」条目，不像正文被切断那样刺眼；且合并思考段会把它们全挤到正文之前（即 Reasonix 形态），是**显示模型的取舍**，不在本刀。**记档备查。**
