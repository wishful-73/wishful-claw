# iter-v2-35 迭代计划（总览）

> 分支 `dev/v2-iter-35`（base `main` @ `991c3019`，v0.2.34）。
> 产品版本目标 `0.2.35` / tag `v0.2.35`（收尾时升）。
> 本文件 = **迭代总览**（目标 / 需求索引 / 刀序 / 门禁基线），≤150 行；需求正文见 `requirements/`，裁定流水见 `changelog.md`。

**当前状态：✅ 已收尾（2026-09-23 14:24 起，发布 `v0.2.35`）** —— 老大 14:24「测试了几轮基本都没问题，把当前的情况看下，进行一次提交 准备发布35迭代，其它登记的没做的，归入36迭代」。本迭代实施并发布 **S-141**（10:48）、**S-142**（11:02 初版 + 11:58 重开）；**未实施项全部移入 [iter-v2-36](../iter-v2-36/plan.md)**（S-129 / S-140 / S-143 / S-144 / S-145）。

---

## 一、目标

1. **S-141**：修「历史会话重启后首次发消息被误触发压缩」——首次加载剥掉快照带入消息的旧 usage，分子只取增量消息的真实值。
2. **S-142**：流式段按 `(iteration, type)` 定位（对齐 Reasonix 的按类型分槽），绝掉「同一段思考流/正文被劈开」这类劈开问题 —— 思考与正文是两条独立通道、分片交错到达，段的身份不能取「到达顺序」。（11:53 重开版；初版 11:01「协议加段边界事件」实测未生效，见 [S-142.md](requirements/S-142.md) §七。）

---

## 二、需求索引

详见 [raw-requirements.md](raw-requirements.md)。

| 编号 | 标题 | 状态 |
|---|---|---|
| S-129 | 更新源全面切换到官网：官网成为完整 electron-updater 源（发布目录 + 清单单一真源） | ➡️ 移入 iter-36（已定稿） |
| S-140 | 更新走了全量下载（108 MB），未走差分 | ➡️ 移入 iter-36（⏸ 挂起：勘测收口三版均无删基准路径；34→35 若再现全量重开） |
| S-141 | 历史会话重启后首次发消息触发压缩：后端加载全历史（≈144K）而非活跃上下文（UI 25K） | ✅ 已实施并发布（`788063e2`，10:48） |
| S-142 | 回合末尾多渲染一个空思考流（同一段思考流被文本劈开） | ✅ 已实施并发布（`7cf7638a` 初版 → `758528d3` 重开） |
| S-143 | 官网上线 + 公安备案（ICP 蜀ICP备2026057067号已通过） | ➡️ 移入 iter-36（站点已上线；公安备案待办） |
| S-144 | 沙箱模式下 Write 写特定路径被拦（中文/下划线/深层子目录，触发项未定位） | ➡️ 移入 iter-36（已登记） |
| S-145 | Read 工具支持读取本地图片（图像直接进模型视觉上下文，不再依赖用户贴图） | ➡️ 移入 iter-36（已定稿） |

---

## 三、刀序

1. S-141 首次加载剥旧 usage —— `788063e2`（1 刀，10:48）
2. S-142 段边界事件（初版）—— `7cf7638a`（1 刀，11:02）
3. S-142 重开：段按 `(iteration, type)` 定位 —— `758528d3`（1 刀，11:58）
4. 迭代收尾：段逻辑抽纯函数模块 + 20 断言回归测试 + 6 条文档订正 + iter-36 承接（1 刀）

---

## 四、门禁基线

| 项 | 基线 |
|---|---|
| `npm run typecheck` | 0 错误 |
| `npm test` | 49 / 49（TS 36 + C# 13） |
| base commit | `991c3019` |
